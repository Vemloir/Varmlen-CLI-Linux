[workspace]
members = ["core", "daemon", "helper", "cli"]
resolver = "2"

[workspace.package]
version = "0.1.2"
edition = "2021"
license = "GPL-3.0-only"
repository = "https://github.com/Vemloir/Varmlen-CLI-Linux"

[profile.release]
strip = "symbols"
lto = true
codegen-units = 1
