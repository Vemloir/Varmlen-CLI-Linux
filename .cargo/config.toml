# Keep build-machine paths out of shipped binaries (see README "Build hygiene").
[build]
rustflags = [
  "--remap-path-prefix=/home/daniil/.cargo=/cargo",
  "--remap-path-prefix=/home/daniil/.rustup=/rustup",
]
